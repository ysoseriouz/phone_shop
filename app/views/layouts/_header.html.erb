<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
	<div class="container-fluid">
		<%= link_to "My Inventory", root_path, class:"navbar-brand" %>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<ul class="navbar-nav me-auto mb-2 mb-lg-0">
					<% if account_signed_in? %>
						<li class="nav-item">
							<%= link_to "New Inventory", new_inventory_path, class:"nav-link" %>
						</li>
						<li class="nav-item">
							<%= link_to "Brand/Model", brand_models_index_path, class:"nav-link" %>
						</li>
					<% end %>
				</ul>
			<div class="d-flex">
				<% if account_signed_in? %>
					<ul class="navbar-nav me-auto mb-2 mb-lg-0">
						<li class="nav-item">
							<%= link_to "Log Out", destroy_account_session_path, method: :delete, class: "nav-link" %>
						</li>
					</ul>
					<%= link_to "#{current_account.role.name}: #{current_account.name}", edit_account_registration_path, class: "nav-link text-white" %>
				<% else %>
					<ul class="navbar-nav me-auto mb-2 mb-lg-0">
						<li class="nav-item">
							<%= link_to "Sign Up", new_account_registration_path, class: "nav-link" %>
						</li>
						<li class="nav-item">
							<%= link_to "Log In", new_account_session_path, class: "nav-link" %>
						</li>
					</ul>
				<% end %>
			</div>
		</div>
	</div>
</nav>
